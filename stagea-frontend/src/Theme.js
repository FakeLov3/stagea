import React from "react";
import { createMuiTheme } from "@material-ui/core";

export default createMuiTheme({
  palette: {
    primary: {
      main: "#506CF0",
    },
    secondary: {
      main: "#FD5E60",
    },
    background: {
      paper: "#F2F2F2",
    },
  },
});
